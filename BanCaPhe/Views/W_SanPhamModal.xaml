<Window x:Class="BanCaPhe.Views.W_SanPhamModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:BanCaPhe.Converters"
        Title="Sản phẩm"
        Height="520"
        Width="600"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CONTENT -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <!-- FORM -->
            <GroupBox Header="Thông tin sản phẩm" Margin="0,0,10,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- TÊN -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Tên đồ uống"/>
                        <TextBox x:Name="txtTen"/>
                    </StackPanel>

                    <!-- GIÁ -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,10">
                        <TextBlock Text="Giá"/>
                        <TextBox x:Name="txtGia"/>
                    </StackPanel>

                    <!-- LOẠI -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,10">
                        <TextBlock Text="Loại đồ uống"/>
                        <ComboBox x:Name="cbLoai"
                                  DisplayMemberPath="TenLoai"
                                  SelectedValuePath="ID"/>
                    </StackPanel>

                    <!-- MÔ TẢ -->
                    <StackPanel Grid.Row="3" Margin="0,0,0,10">
                        <TextBlock Text="Mô tả"/>
                        <TextBox x:Name="txtMoTa"
                                 Height="80"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"/>
                    </StackPanel>

                    <!-- CÒN BÁN -->
                    <CheckBox x:Name="chkConBan"
                              Grid.Row="4"
                              Content="Còn bán"/>
                </Grid>
            </GroupBox>

            <!-- IMAGE -->
            <GroupBox Grid.Column="1" Header="Hình ảnh">
                <StackPanel Margin="10" HorizontalAlignment="Center">
                    <Border BorderBrush="Gray"
                            BorderThickness="1"
                            CornerRadius="6"
                            Width="160"
                            Height="160">
                        <Image x:Name="imgPreview" Stretch="Uniform"/>
                    </Border>

                    <Button Content="Chọn hình"
                            Width="120"
                            Margin="0,10,0,0"
                            Click="BtnChonHinh_Click"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Lưu"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="BtnLuu_Click"/>
            <Button Content="Hủy"
                    Width="90"
                    Click="BtnHuy_Click"/>
        </StackPanel>
    </Grid>
</Window>
